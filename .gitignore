# Entorno virtual
venv/
# Carpeta de datos (imágenes)
#data/
# Archivos compilados y caché
__pycache__/
*.pyc
# Metadata generada por `pip install -e .` (solo aplica en feature/pest-detection-package)
*.egg-info/
# Modelos y checkpoints
*.h5
*.tif
*.zip
.DS_Store
# Logs crudos de las corridas de reentrenamiento (evidencia de desarrollo, no de
# repo) - ver BITACORA_RETRAIN_2026-07.md para el resumen curado con lo relevante.
retrain_logs/